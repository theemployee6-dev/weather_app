name: weatherbloc1_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  geolocator: ^11.0.0
  flutter_bloc: 9.0.0
  intl: ^0.19.0
  equatable: ^2.0.5
  weather: ^3.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.1.3
  internet_connection_checker: ^3.0.1
  analyzer: ^6.11.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner: ^2.4.15
  hive_generator: ^2.0.1


flutter:
  uses-material-design: true

  assets:
    - assets/
